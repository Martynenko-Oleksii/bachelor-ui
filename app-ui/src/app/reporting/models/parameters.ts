export interface TimePeriod {
    id: number,
    shortName: string;
}

export interface Department {
    id: number;
    name: string;
}

export interface Measure {
    id: number;
    name: string;
}